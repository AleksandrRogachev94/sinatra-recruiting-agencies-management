<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Recruiting Agencies</title>
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/stylesheets/stylesheet.css">
  </head>
  <body>
    <div class="container">
      <section>

        <div class="my_navbar">
          <div class="left-bar">
            <a href="/" class="btn btn-default" role="button">Main Page</a>
            <% if logged_in_agency? %>
              <a href="/" class="btn btn-default" role="button">Recruiters</a>
              <a href="/" class="btn btn-default" role="button">Vacancies</a>
              <a href="/" class="btn btn-default" role="button">Companies</a>
            <% end %>
            <% if logged_in_recruiter? %>
              <a href="/" class="btn btn-default" role="button">Vacancies</a>
            <% end %>
          </div>
          <div class="right-bar">
            <% if logged_in? %>
              <h4><%= current_user.username %></h4>
              <a href="/logout" class="btn btn-default" role="button">Log Out</a>
            <% else %>
              <a href="/agency/login" class="btn btn-default" role="button">Log In (Agency)</a>
              <a href="/recruiter/login" class="btn btn-default" role="button">Log In (Recruiter)</a>
              <a href="#" class="btn btn-default" role="button">Sign Up</a>
            <% end %>
          </div>
        </div>

        <%= yield %>
      </section>

      <footer>
        &copy Aleksandr Rogachev, 2017
      </footer>
    </div>
  </body>
</html>
